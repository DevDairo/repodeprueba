services:
  backend:
    build:
      context: ../backend
      dockerfile: Dockerfile
    container_name: kontora_pos_backend
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:8080:8080"
